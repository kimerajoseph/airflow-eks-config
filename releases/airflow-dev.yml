apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: airflow
  namespace: staging
  annotations:
    # information used by external libbraries
    flux.weave.works/automated: "true"
spec:
  releaseName: airflow-dev
  chart:
    #repository: https://marclamberti.github.io/airflow-eks-helm-chart
    repository: https://airflow.apache.org
    #repository: https://github.com/kimerajoseph/airflow-eks-helm-chart-marclambert.git
    name: airflow
    version: 1.0.0
  values:
    # used to encrypt connections
    fernetKey: "zTxhk7FuGqQVPZVoukLn5bGWOrgHzhQnBnaqAItcZGI="
    defaultAirflowTag: "1.10.10-2-python3.7"
    env:
      # modifying the airflow.cfg file
      # under k8s >> dags_in_image parameter
      # each pod runs its own airflow
      - name: "AIRFLOW__KUBERNETES__DAGS_IN_IMAGE"
        value: "True"
      - name: "AIRFLOW__KUBERNETES__NAMESPACE"
        value: "dev"
      - name: "AIRFLOW__KUBERNETES__WORKER_CONTAINER_REPOSITORY"
        value: "apache/airflow"
      - name: "AIRFLOW__KUBERNETES__WORKER_CONTAINER_TAG"
        value: "1.10.10-2-python3.7"
      - name: "AIRFLOW__KUBERNETES__RUN_AS_USER"
        value: "50000"
      - name: "AIRFLOW__CORE__LOAD_EXAMPLES"  # load examples. set to False if examples not needed
        value: "True"
      # AIRFLOW__KUBERNETES__DAGS_IN_IMAGE: "True"
      # AIRFLOW__KUBERNETES__NAMESPACE: "dev"
      # AIRFLOW__KUBERNETES__WORKER_CONTAINER_REPOSITORY: "apache/airflow"
      # AIRFLOW__KUBERNETES__WORKER_CONTAINER_TAG: "1.10.10.1-alpha2-python3.7"
      # # pods run under user airflow
      # AIRFLOW__KUBERNETES__RUN_AS_USER: "50000"
      # AIRFLOW__CORE__LOAD_EXAMPLES: "False"
    executor: "KubernetesExecutor"
    